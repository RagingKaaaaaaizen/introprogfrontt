{"ast":null,"code":"import { forkJoin, of } from 'rxjs';\nimport { map, catchError } from 'rxjs/operators';\nimport { environment } from '@environments/environment';\nimport * as i0 from \"@angular/core\";\nimport * as i1 from \"@angular/common/http\";\nimport * as i2 from \"./stock.service\";\nimport * as i3 from \"./item.service\";\nimport * as i4 from \"./pc.service\";\nimport * as i5 from \"./employee.service\";\nimport * as i6 from \"./department.service\";\nimport * as i7 from \"./dispose.service\";\nexport let AnalyticsService = /*#__PURE__*/(() => {\n  class AnalyticsService {\n    constructor(http, stockService, itemService, pcService, employeeService, departmentService, disposeService) {\n      this.http = http;\n      this.stockService = stockService;\n      this.itemService = itemService;\n      this.pcService = pcService;\n      this.employeeService = employeeService;\n      this.departmentService = departmentService;\n      this.disposeService = disposeService;\n      this.baseUrl = environment.apiUrl;\n    }\n    getDashboardAnalytics() {\n      return forkJoin({\n        stocks: this.stockService.getAll().pipe(catchError(() => of([]))),\n        items: this.itemService.getAll().pipe(catchError(() => of([]))),\n        pcs: this.pcService.getAll().pipe(catchError(() => of([]))),\n        employees: this.employeeService.getAll().pipe(catchError(() => of([]))),\n        departments: this.departmentService.getAll().pipe(catchError(() => of([]))),\n        disposals: this.disposeService.getAll().pipe(catchError(() => of([])))\n      }).pipe(map(data => this.processAnalyticsData(data)));\n    }\n    getTimelineData(days = 30) {\n      return forkJoin({\n        stocks: this.stockService.getAll().pipe(catchError(() => of([]))),\n        items: this.itemService.getAll().pipe(catchError(() => of([]))),\n        disposals: this.disposeService.getAll().pipe(catchError(() => of([]))),\n        pcs: this.pcService.getAll().pipe(catchError(() => of([])))\n      }).pipe(map(data => this.processTimelineData(data, days)));\n    }\n    getStockTimelineByCategory(days = 30) {\n      return this.stockService.getAll().pipe(map(stocks => this.processStockTimelineData(stocks, days)), catchError(() => of([])));\n    }\n    getStockAdditionsOverTime(days = 30) {\n      return this.stockService.getAll().pipe(map(stocks => this.processStockAdditionsData(stocks, days)), catchError(() => of([])));\n    }\n    getStockDisposalsOverTime(days = 30) {\n      return this.disposeService.getAll().pipe(map(disposals => this.processStockDisposalsData(disposals, days)), catchError(() => of([])));\n    }\n    processAnalyticsData(data) {\n      const stocks = data.stocks || [];\n      const items = data.items || [];\n      const pcs = data.pcs || [];\n      const employees = data.employees || [];\n      const departments = data.departments || [];\n      const disposals = data.disposals || [];\n      // Calculate stock by category\n      const stockByCategory = {};\n      stocks.forEach(stock => {\n        const item = items.find(i => i.id === stock.itemId);\n        if (item && item.category) {\n          stockByCategory[item.category.name] = (stockByCategory[item.category.name] || 0) + stock.quantity;\n        }\n      });\n      // Calculate stock by location\n      const stockByLocation = {};\n      stocks.forEach(stock => {\n        if (stock.storageLocation) {\n          stockByLocation[stock.storageLocation.name] = (stockByLocation[stock.storageLocation.name] || 0) + stock.quantity;\n        }\n      });\n      // Find low stock items (less than 10)\n      const lowStockItems = stocks.filter(stock => stock.quantity < 10 && stock.quantity > 0).length;\n      // Find out of stock items\n      const outOfStockItems = stocks.filter(stock => stock.quantity === 0).length;\n      // Calculate total stock quantity\n      const totalStock = stocks.reduce((sum, stock) => sum + (stock.quantity || 0), 0);\n      return {\n        totalItems: items.length,\n        totalStock: totalStock,\n        totalPCs: pcs.length,\n        totalEmployees: employees.length,\n        totalDepartments: departments.length,\n        totalDisposals: disposals.length,\n        lowStockItems,\n        outOfStockItems,\n        stockByCategory,\n        stockByLocation,\n        recentActivity: this.getRecentActivity(stocks, items, disposals)\n      };\n    }\n    processTimelineData(data, days) {\n      const timeline = [];\n      const today = new Date();\n      for (let i = days - 1; i >= 0; i--) {\n        const date = new Date(today);\n        date.setDate(date.getDate() - i);\n        const dateStr = date.toISOString().split('T')[0];\n        // Simulate historical data (in a real app, you'd query by date)\n        const randomFactor = 0.8 + Math.random() * 0.4; // 0.8 to 1.2\n        timeline.push({\n          date: dateStr,\n          stockCount: Math.floor((data.stocks?.length || 0) * randomFactor),\n          itemCount: Math.floor((data.items?.length || 0) * randomFactor),\n          disposalCount: Math.floor((data.disposals?.length || 0) * randomFactor),\n          pcCount: Math.floor((data.pcs?.length || 0) * randomFactor)\n        });\n      }\n      return timeline;\n    }\n    processStockTimelineData(stocks, days) {\n      const timeline = [];\n      const today = new Date();\n      const startDate = new Date(today);\n      startDate.setDate(startDate.getDate() - days);\n      // Filter stocks that were created within the specified days\n      const recentStocks = stocks.filter(stock => {\n        if (!stock.createdAt) return false;\n        const stockDate = new Date(stock.createdAt);\n        return stockDate >= startDate && stockDate <= today;\n      });\n      // Group stocks by date and category\n      const dateCategoryMap = new Map();\n      recentStocks.forEach(stock => {\n        const stockDate = new Date(stock.createdAt);\n        const dateStr = stockDate.toISOString().split('T')[0];\n        const category = stock.item?.category?.name || 'Unknown';\n        if (!dateCategoryMap.has(dateStr)) {\n          dateCategoryMap.set(dateStr, new Map());\n        }\n        const categoryMap = dateCategoryMap.get(dateStr);\n        const currentQuantity = categoryMap.get(category) || 0;\n        categoryMap.set(category, currentQuantity + (stock.quantity || 0));\n      });\n      // Convert to timeline data format\n      dateCategoryMap.forEach((categoryMap, dateStr) => {\n        categoryMap.forEach((quantity, category) => {\n          if (category !== 'Unknown' && quantity > 0) {\n            timeline.push({\n              date: dateStr,\n              category: category,\n              quantity: quantity\n            });\n          }\n        });\n      });\n      // Sort by date\n      timeline.sort((a, b) => new Date(a.date).getTime() - new Date(b.date).getTime());\n      return timeline;\n    }\n    processStockAdditionsData(stocks, days) {\n      const timeline = [];\n      const today = new Date();\n      const startDate = new Date(today);\n      startDate.setDate(startDate.getDate() - days);\n      const dailyAdditions = new Map();\n      stocks.filter(stock => {\n        if (!stock.createdAt) return false;\n        const stockDate = new Date(stock.createdAt);\n        return stockDate >= startDate && stockDate <= today;\n      }).forEach(stock => {\n        const stockDate = new Date(stock.createdAt);\n        const dateStr = stockDate.toISOString().split('T')[0];\n        dailyAdditions.set(dateStr, (dailyAdditions.get(dateStr) || 0) + (stock.quantity || 0));\n      });\n      const sortedDates = Array.from(dailyAdditions.keys()).sort((a, b) => new Date(a).getTime() - new Date(b).getTime());\n      sortedDates.forEach(date => {\n        timeline.push({\n          date: date,\n          stockCount: dailyAdditions.get(date) || 0,\n          itemCount: dailyAdditions.get(date) || 0,\n          // Assuming itemCount is the same as stockCount for additions\n          disposalCount: 0,\n          // No disposals in this context\n          pcCount: 0 // No PCs in this context\n        });\n      });\n      return timeline;\n    }\n    processStockDisposalsData(disposals, days) {\n      const timeline = [];\n      const today = new Date();\n      const startDate = new Date(today);\n      startDate.setDate(startDate.getDate() - days);\n      const dailyDisposals = new Map();\n      disposals.filter(disposal => {\n        if (!disposal.disposalDate) return false;\n        const disposalDate = new Date(disposal.disposalDate);\n        return disposalDate >= startDate && disposalDate <= today;\n      }).forEach(disposal => {\n        const disposalDate = new Date(disposal.disposalDate);\n        const dateStr = disposalDate.toISOString().split('T')[0];\n        dailyDisposals.set(dateStr, (dailyDisposals.get(dateStr) || 0) + (disposal.quantity || 1));\n      });\n      const sortedDates = Array.from(dailyDisposals.keys()).sort((a, b) => new Date(a).getTime() - new Date(b).getTime());\n      sortedDates.forEach(date => {\n        timeline.push({\n          date: date,\n          stockCount: 0,\n          itemCount: 0,\n          disposalCount: dailyDisposals.get(date) || 0,\n          pcCount: 0\n        });\n      });\n      return timeline;\n    }\n    getRecentActivity(stocks, items, disposals) {\n      const activities = [];\n      // Add recent stock changes\n      stocks.slice(0, 5).forEach(stock => {\n        const item = items.find(i => i.id === stock.itemId);\n        if (item) {\n          activities.push({\n            type: 'stock',\n            message: `${item.name} stock updated to ${stock.quantity}`,\n            timestamp: new Date(),\n            icon: 'fas fa-boxes'\n          });\n        }\n      });\n      // Add recent disposals\n      disposals.slice(0, 3).forEach(disposal => {\n        activities.push({\n          type: 'disposal',\n          message: `${disposal.itemName || 'Item'} disposed`,\n          timestamp: new Date(disposal.createdAt || new Date()),\n          icon: 'fas fa-trash-alt'\n        });\n      });\n      return activities.sort((a, b) => b.timestamp.getTime() - a.timestamp.getTime()).slice(0, 8);\n    }\n    getStockAlerts() {\n      return this.stockService.getAll().pipe(map(stocks => {\n        return stocks.filter(stock => stock.quantity < 10).map(stock => ({\n          itemId: stock.itemId,\n          quantity: stock.quantity,\n          alert: stock.quantity === 0 ? 'Out of Stock' : 'Low Stock'\n        })).slice(0, 10);\n      }), catchError(() => of([])));\n    }\n    getCategoryDistribution() {\n      return this.stockService.getAll().pipe(map(stocks => {\n        const categories = {};\n        // Group stocks by category and sum quantities\n        stocks.forEach(stock => {\n          if (stock.item && stock.item.category && stock.item.category.name) {\n            const categoryName = stock.item.category.name;\n            categories[categoryName] = (categories[categoryName] || 0) + (stock.quantity || 0);\n          }\n        });\n        return Object.entries(categories).map(([name, totalStock]) => ({\n          name,\n          count: totalStock,\n          percentage: 0 // Will be calculated in the component\n        }));\n      }), catchError(() => of([])));\n    }\n    static #_ = this.ɵfac = function AnalyticsService_Factory(t) {\n      return new (t || AnalyticsService)(i0.ɵɵinject(i1.HttpClient), i0.ɵɵinject(i2.StockService), i0.ɵɵinject(i3.ItemService), i0.ɵɵinject(i4.PCService), i0.ɵɵinject(i5.EmployeeService), i0.ɵɵinject(i6.DepartmentService), i0.ɵɵinject(i7.DisposeService));\n    };\n    static #_2 = this.ɵprov = /*@__PURE__*/i0.ɵɵdefineInjectable({\n      token: AnalyticsService,\n      factory: AnalyticsService.ɵfac,\n      providedIn: 'root'\n    });\n  }\n  return AnalyticsService;\n})();","map":null,"metadata":{},"sourceType":"module","externalDependencies":[]}